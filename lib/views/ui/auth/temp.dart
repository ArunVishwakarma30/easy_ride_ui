import 'package:flutter/material.dart';

class hehe extends StatelessWidget {
  const hehe({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text("data"*1000),);
  }
}